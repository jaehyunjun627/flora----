# Flora 프로젝트 - 아쉬운 점 & 개선점 분석

> 코드 및 설정 파일 전체 검토 기준 (2026.04.20)

---

## 심각도 분류

- 🔴 **Critical** - 실제 서비스라면 즉시 수정 필요 (보안/데이터 손실 위험)
- 🟡 **Important** - 유지보수 및 품질에 직접 영향
- 🟢 **Nice to have** - 구조적 개선, 장기적 관점

---

## 1. 보안 문제 🔴

### 1-1. 민감 정보가 코드에 하드코딩됨

**현재 (application.yml)**
```yaml
jwt:
  secret: FloraSecretKey2026FloraSecretKey2026   # ← Git에 노출됨

datasource:
  username: system    # ← Oracle 관리자 계정 직접 사용
  password: 12345     # ← 너무 단순한 비밀번호

external:
  pixabay:
    api-key: 3956381-8a0f2a1805bed555538d1bfe8   # ← 실제 키 노출
  forest:
    api-key: 2c771237c3e6476b1eb...              # ← 실제 키 노출
```

**문제점**
- Git에 커밋되면 GitHub에 API 키가 영구 노출
- Oracle `system` 계정은 DB 전체 관리자 권한 → 앱이 해킹되면 DB 전체 위험
- JWT secret이 짧고 예측 가능 → 토큰 위조 가능

**개선 방법**
```yaml
# application.yml - 환경변수로 분리
jwt:
  secret: ${JWT_SECRET}          # 환경변수에서 읽기

datasource:
  username: ${DB_USERNAME}
  password: ${DB_PASSWORD}

external:
  pixabay:
    api-key: ${PIXABAY_API_KEY}
```
```bash
# .env 파일 (Git에 포함 X, .gitignore에 추가)
JWT_SECRET=랜덤하고_긴_문자열_최소64자
DB_USERNAME=flora_app           # 전용 계정 생성
DB_PASSWORD=복잡한비밀번호
```

---

### 1-2. Refresh Token 없음

**현재 흐름**
```
로그인 → Access Token (24시간) → 만료 → 강제 로그아웃
```

**문제점**: 24시간짜리 토큰 하나만 사용
- 토큰이 탈취되면 24시간 동안 아무 조치 불가
- 매일 다시 로그인해야 하는 불편함

**개선 방법**
```
로그인 → Access Token (30분) + Refresh Token (14일)
       → Access Token 만료 시 Refresh Token으로 자동 갱신
       → Refresh Token도 만료 시에만 재로그인
       → 의심스러운 활동 시 Refresh Token만 삭제 → 즉시 강제 로그아웃 가능
```

---

### 1-3. 프론트엔드 라우트 보호 없음

**현재 App.jsx**
```jsx
// 로그인 없이도 아래 경로 접근 가능 (URL 직접 입력)
<Route path="/seller" element={<SellerDashboardPage />} />
<Route path="/mypage" element={<MyPage />} />
<Route path="/checkout" element={<CheckoutPage />} />
```

**문제점**: 백엔드가 막아주긴 하지만, 로그인 안 한 사용자가 판매자 대시보드로 직접 접근하면 에러 화면이 뜸

**개선 방법**
```jsx
// ProtectedRoute 컴포넌트 만들기
function ProtectedRoute({ children, requiredRole }) {
  const { user, token } = useContext(AuthContext);
  if (!token) return <Navigate to="/login" />;
  if (requiredRole && user?.role !== requiredRole) return <Navigate to="/" />;
  return children;
}

// 사용
<Route path="/seller" element={
  <ProtectedRoute requiredRole="SELLER">
    <SellerDashboardPage />
  </ProtectedRoute>
} />
```

---

## 2. 데이터베이스 위험 🔴

### 2-1. `ddl-auto: update` 운영 사용 금지

**현재**
```yaml
jpa:
  hibernate:
    ddl-auto: update   # ← 매우 위험
```

**문제점**
- `update` = Hibernate가 Entity 변경 감지해서 자동으로 ALTER TABLE 실행
- 컬럼 추가는 되지만, **컬럼 삭제/이름 변경은 반영 안 됨** → 데이터 불일치
- 팀원이 Entity 잘못 수정하면 서버 시작 시 테이블 구조 자동 변경 → 데이터 손실 위험

**개선 방법**
```yaml
# 개발 환경
ddl-auto: create-drop  # 시작 시 생성, 종료 시 삭제

# 운영 환경
ddl-auto: validate     # Entity와 DB 구조가 다르면 시작 실패 (변경은 직접 SQL로)
```
장기적으로는 **Flyway** 또는 **Liquibase** 도입 (DB 변경 이력 관리)
```
V1__create_users_table.sql
V2__add_profile_column.sql
V3__create_products_table.sql
```

---

### 2-2. `show-sql: true` 운영에서 제거 필요

```yaml
show-sql: true   # 모든 SQL이 콘솔에 출력 → 운영 서버 성능 저하, 로그 오염
```
→ 개발 환경에서만 활성화, 운영은 `false`

---

## 3. 테스트 코드 전무 🔴

**현재**: 백엔드, 프론트 모두 테스트 파일 0개

**문제점**
- 기능 수정 시 다른 기능이 깨졌는지 알 방법이 없음
- 리팩토링이 무서워짐
- 버그가 개발 단계가 아닌 사용자가 발견

**개선 방법 (우선순위)**
```java
// 1순위: Service 단위 테스트
@ExtendWith(MockitoExtension.class)
class OrderServiceTest {
    @Mock
    OrderRepository orderRepository;
    @Mock
    ProductRepository productRepository;
    @InjectMocks
    OrderService orderService;

    @Test
    void 재고부족시_주문실패() {
        // given
        Product product = Product.builder().stock(0).build();
        when(productRepository.findById(1L)).thenReturn(Optional.of(product));

        // when & then
        assertThrows(RuntimeException.class,
            () -> orderService.createOrder(1L, 1L, 1));
    }
}
```

---

## 4. 백엔드 코드 품질 🟡

### 4-1. 입력값 검증(@Valid) 없음

**현재 패턴 (추정)**
```java
@PostMapping
public ResponseEntity<?> createProduct(@RequestBody ProductDto dto) {
    // 검증 없이 바로 Service 호출
    return ResponseEntity.ok(productService.create(dto));
}
```

**문제점**: 이름이 빈 상품, 가격이 음수인 상품 등록 가능

**개선 방법**
```java
// DTO에 검증 어노테이션 추가
public class ProductDto {
    @NotBlank(message = "상품명은 필수입니다")
    private String name;

    @Min(value = 0, message = "가격은 0 이상이어야 합니다")
    private int price;

    @Max(value = 99999, message = "재고는 99999개 이하")
    private int stock;
}

// Controller에 @Valid 추가
@PostMapping
public ResponseEntity<?> createProduct(@Valid @RequestBody ProductDto dto) { ... }
```

---

### 4-2. API 버전 관리 없음

**현재**
```
/api/products
/api/orders
```

**문제점**: 나중에 API 구조를 바꾸면 기존 앱이 일괄 깨짐

**개선 방법**
```
/api/v1/products   ← 현재 버전 유지
/api/v2/products   ← 새 구조 (기존 앱에 영향 없음)
```

---

### 4-3. 환경 분리 없음 (dev/prod 프로파일)

**현재**: 설정 파일이 application.yml 하나

**개선 방법**
```
application.yml           ← 공통 설정
application-dev.yml       ← 개발: show-sql=true, ddl-auto=create-drop
application-prod.yml      ← 운영: show-sql=false, ddl-auto=validate
```
```bash
# 실행 시 프로파일 지정
./gradlew bootRun --args='--spring.profiles.active=dev'
```

---

### 4-4. Service 인터페이스 없음

**현재**
```java
// 구현 클래스만 존재
@Service
public class ProductService { ... }
```

**개선 방법**
```java
// 인터페이스 정의
public interface ProductService {
    Page<ProductDto> getProducts(Pageable pageable, String category);
    ProductDto createProduct(ProductDto dto, Long sellerId);
}

// 구현체
@Service
public class ProductServiceImpl implements ProductService { ... }
```
→ 테스트 시 Mock 교체 용이, 구현 교체 가능

---

## 5. 프론트엔드 코드 품질 🟡

### 5-1. CSS 파일 54개 (페이지당 1개씩)

**현재**
```
HomePage.jsx + HomePage.css
ProductListPage.jsx + ProductListPage.css
CartPage.jsx + CartPage.css
... (27쌍)
```

**문제점**: 공통 스타일(버튼, 카드, 폼 등)이 각 파일에 중복 → 수정 시 54개 파일 찾아다녀야 함

**개선 방법 (단기)**: 공통 변수와 클래스를 `global.css`로 분리
```css
/* global.css */
:root {
  --primary: #4CAF50;
  --danger: #f44336;
  --card-radius: 12px;
}
.btn-primary { ... }
.card { ... }
.form-input { ... }
```

---

### 5-2. 로딩/에러 처리 패턴이 페이지마다 제각각

**현재 (추정 패턴)**
```jsx
// 각 페이지마다 동일한 패턴 반복
const [loading, setLoading] = useState(false);
const [error, setError] = useState(null);

useEffect(() => {
    setLoading(true);
    try {
        const data = await api.getProducts();
        setProducts(data);
    } catch (e) {
        setError(e.message);
    } finally {
        setLoading(false);
    }
}, []);

if (loading) return <div>로딩중...</div>;
if (error) return <div>에러: {error}</div>;
```

**개선 방법**: 커스텀 훅으로 추출
```jsx
// hooks/useApi.js
function useApi(apiFn, deps = []) {
    const [data, setData] = useState(null);
    const [loading, setLoading] = useState(true);
    const [error, setError] = useState(null);

    useEffect(() => {
        setLoading(true);
        apiFn()
            .then(setData)
            .catch(e => setError(e.message))
            .finally(() => setLoading(false));
    }, deps);

    return { data, loading, error };
}

// 사용 (한 줄로 해결)
const { data: products, loading, error } = useApi(() => productApi.getProducts());
```

---

### 5-3. TypeScript 미사용

**현재**: 순수 JavaScript

**문제점**
- `product.pric` (오타) 같은 실수를 런타임이 아니면 발견 못함
- 함수 파라미터 타입을 모르면 코드를 열어봐야 함
- 팀 협업 시 인터페이스 파악이 어려움

**개선 방향**: 신규 파일부터 `.ts` / `.tsx`로 작성, 점진적 전환

---

## 6. 구조적 개선점 🟢

### 6-1. 에러 응답 형식 통일

**현재 (추정)**: 에러 메시지가 문자열로 반환
```json
"이미 사용중인 이메일입니다"
```

**개선**: 에러 코드 포함한 구조화된 응답
```json
{
  "code": "EMAIL_DUPLICATED",
  "message": "이미 사용중인 이메일입니다",
  "timestamp": "2026-04-20T10:30:00"
}
```
→ 프론트에서 `code` 기준으로 다국어 처리, 특정 에러에 따른 UI 분기 가능

---

### 6-2. 파일 업로드 경로 하드코딩

**현재**
```java
Path path = Paths.get("uploads/" + fileName);
```

**문제점**: 서버 재시작 시 현재 디렉토리 기준으로 저장 → 배포 환경에 따라 경로가 달라짐

**개선**
```yaml
# application.yml
file:
  upload-dir: ${UPLOAD_DIR:/app/uploads}  # 환경변수로 관리
```

---

### 6-3. ChatBot과 외부 API 연동 완성도

**현재**: ChatBot.jsx가 존재하지만 OpenAI API key가 `YOUR_OPENAI_API_KEY_HERE`로 미설정

**개선**: 실제 키 환경변수 연동 + 식물 진단 기능 완성

---

## 전체 요약표

| 분류 | 문제 | 심각도 | 난이도 |
|------|------|--------|--------|
| 보안 | API 키/비밀번호 하드코딩 | 🔴 Critical | 쉬움 |
| 보안 | Oracle system 계정 사용 | 🔴 Critical | 쉬움 |
| 보안 | Refresh Token 없음 | 🔴 Critical | 보통 |
| 보안 | 프론트 라우트 보호 없음 | 🔴 Critical | 쉬움 |
| DB | `ddl-auto: update` | 🔴 Critical | 쉬움 |
| 테스트 | 테스트 코드 0개 | 🔴 Critical | 어려움 |
| 백엔드 | 입력값 검증 없음 | 🟡 Important | 쉬움 |
| 백엔드 | 환경 분리 없음 (dev/prod) | 🟡 Important | 쉬움 |
| 백엔드 | API 버전 관리 없음 | 🟡 Important | 보통 |
| 프론트 | 라우트 보호 컴포넌트 없음 | 🟡 Important | 쉬움 |
| 프론트 | CSS 파일 분산/중복 | 🟡 Important | 보통 |
| 프론트 | 로딩/에러 처리 중복 | 🟡 Important | 보통 |
| 프론트 | TypeScript 미사용 | 🟢 Nice to have | 어려움 |
| 백엔드 | Service 인터페이스 없음 | 🟢 Nice to have | 보통 |
| 공통 | 에러 응답 형식 미통일 | 🟢 Nice to have | 쉬움 |

---

## 당장 할 수 있는 우선 개선 (1~2일)

1. `.env` 파일 만들고 `application.yml` 환경변수 분리 + `.gitignore`에 추가
2. `ddl-auto: validate` 로 변경 (현재 테이블 구조 고정)
3. `ProtectedRoute` 컴포넌트 추가 (프론트 라우트 보호)
4. Oracle 전용 앱 계정 생성 (`CREATE USER flora_app IDENTIFIED BY ...`)
5. `show-sql: false` (운영 환경)
